import { css } from "@emotion/css";

const defaultStyles = () => css`
  &.trigger {
    display: inline-block;
    width: 100%;
  }

  &.overlay {
    display: block;
  }
`;

export default defaultStyles;
