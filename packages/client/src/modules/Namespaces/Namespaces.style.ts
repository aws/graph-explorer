import { css } from "@emotion/css";

const defaultStyles = () =>
  css`
    position: relative;
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: auto;
  `;

export default defaultStyles;
