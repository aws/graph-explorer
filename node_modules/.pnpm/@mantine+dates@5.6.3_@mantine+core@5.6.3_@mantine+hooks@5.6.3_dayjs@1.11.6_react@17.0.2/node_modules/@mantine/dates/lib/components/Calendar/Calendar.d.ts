/// <reference types="react" />
import { CalendarBaseProps } from '../CalendarBase/CalendarBase';
export interface CalendarProps<Multiple extends boolean = false> extends Omit<CalendarBaseProps, 'value' | 'onChange' | 'isDateInRange' | 'isDateFirstInRange' | 'isDateLastInRange'> {
    multiple?: Multiple;
    value?: Multiple extends true ? Date[] : Date | null;
    onChange?(value: Multiple extends true ? Date[] : Date | null): void;
}
export declare function Calendar<Multiple extends boolean = false>(props: CalendarProps<Multiple>): JSX.Element;
export declare namespace Calendar {
    var displayName: string;
}
//# sourceMappingURL=Calendar.d.ts.map