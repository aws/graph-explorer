import { Action } from '../action';
import { ColdSubscription } from '../action/types';
declare type ActionMap = {
    [key: string]: Action;
};
declare const composite: (actions: ActionMap) => Action<ColdSubscription>;
export default composite;
