import Chainable from '../chainable';
import { ObserverCandidate } from '../observer/types';
import { ActionInit, ActionProps, ColdSubscription } from './types';
export declare class Action<Sub extends ColdSubscription = ColdSubscription> extends Chainable<Action> {
    create(props: ActionProps): Action<ColdSubscription>;
    start(observerCandidate?: ObserverCandidate): Sub;
}
declare const _default: <Sub extends ColdSubscription = ColdSubscription>(init: ActionInit) => Action<Sub>;
export default _default;
