import { MotionProps } from "../../../motion/types";
import { HTMLVisualElement } from "../HTMLVisualElement";
export declare function buildHTMLProps(visualElement: HTMLVisualElement, { drag }: MotionProps): any;
